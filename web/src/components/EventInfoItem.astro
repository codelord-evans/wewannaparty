---
interface Props {
  label: string;
  value: string;
}

const { label, value } = Astro.props;
---

<div class="rounded-xl bg-muted/30 p-4">
  <p class="mb-1 text-sm text-muted-foreground">{label}</p>
  <p class="font-medium text-foreground">{value}</p>
</div>
